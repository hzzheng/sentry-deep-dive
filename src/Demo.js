import React, { Component } from 'react';
import Error from './Error';

export default class Demo extends Component {
  
  render() {
    return (
      <div>
        <Error />
      </div>
    );
  }
}